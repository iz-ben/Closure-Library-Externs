

var plupload = {};

/**
 * @constructor
 * @param {*} config
 */

plupload.Uploader = function(config){};

plupload.Uploader.prototype.init = function(){};

/**
 * @param {string} event
 * @param {Function} callback
 *
 */
plupload.Uploader.prototype.bind = function( event, callback ){};


/**
 * @param {File|Node|Array} file
 * @param {string} fileName
 *
 */
plupload.Uploader.prototype.addFile = function( file, fileName ){};

plupload.Uploader.prototype.start = function(){};

plupload.Uploader.prototype.stop = function(){};

